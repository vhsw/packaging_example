[project]
name = "package-a"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "helpers",
]

[tool.uv.sources]
helpers = { path = "../helpers" }
